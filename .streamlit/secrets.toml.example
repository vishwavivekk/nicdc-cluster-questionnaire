# Copy this file to `.streamlit/secrets.toml` for local development OR
# paste these key/value pairs into Streamlit Community Cloud → App → Settings → Secrets.

admin_password = "change-me-to-a-strong-password"
